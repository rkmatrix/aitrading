mode: PAPER
policy_name: EquityRLPolicy

paths:
  policy_path: models/policies/EquityRLPolicy/current_policy/model
  save_path: models/policies/EquityRLPolicy/current_policy/model
  regime_state_file: data/runtime/phase59_regime_state.json

training:
  lr: 0.00005
  batch_size: 64
  gradient_steps: 200

online_finetune:
  max_buffer_size: 512
  batch_size: 32
  mini_updates: 4
  update_interval_steps: 20
  min_buffer_for_update: 64
  lr_scale_risk_off: 0.3
  skip_in_risk_off: false
