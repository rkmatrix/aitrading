name: execution_base
version: "1.1.0"
schema: execution_policy_v1
kind: execution
payload:
  slippage_bps: 12              # Simulated slippage (basis points)
  delay_ms: 120                 # Simulated broker latency in ms
  max_retry: 3
  cancel_on_fail: true
  brokers:
    primary: alpaca
    backup: backup
  risk_filters:
    max_order_value_usd: 25000
    max_daily_trades: 50
metadata:
  description: Default execution parameters for RL-driven agents.
  created_by: system
  last_updated: 2025-11-16
source_id: local-default
